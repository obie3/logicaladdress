import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
  image: {
    width: '100%',
    height: 50,
    resizeMode: 'contain',
    justifyContent: 'center',
    alignItems: 'center',
  },
  LogoLayout: {
    height: 100,
    width: '100%',
    alignItems: 'center',
    justifyContent: 'center',
  },
});
